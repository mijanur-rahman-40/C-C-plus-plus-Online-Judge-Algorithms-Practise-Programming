#include < stdio.h >

int main(){
    int tst  , cas = 0;
    scanf("%d",&tst);
    ///while(tst--)
    for(int i = 1; i <= tst ;i++){
        int  n , marks;
        scanf("%d",&marks);
        printf("Case %d: ",++cas);

        if(marks >= 80)pf("A+");
        else if(marks >= 75 && marks < 80)pf("A");
        else if(marks >)

        printf("\n");
    }
    return 0;
}
